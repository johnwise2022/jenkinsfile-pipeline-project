pipeline {
    agent any
    stages {
        stage('run') {
            steps {
                echo 'umarım yakındır'
                sh 'python --version'
                sh 'python pipeline.py'
            }
        }
    }
}
